import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath}from'node:url';import {spawn}from'node:child_process';
const base=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../www');
const types={'.html':'text/html; charset=utf-8','.js':'application/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.webp':'image/webp','.png':'image/png','.mp3':'audio/mpeg','.json':'application/json','.webmanifest':'application/manifest+json'};
const server=http.createServer((req,res)=>{let p;try{p=decodeURIComponent(new URL(req.url,'http://localhost').pathname)}catch{res.writeHead(400);return res.end('Bad request')}if(p==='/')p='/index.html';const f=path.resolve(base,'.'+p);if(!f.startsWith(base+path.sep)){res.writeHead(403);return res.end()}fs.stat(f,(e,s)=>{if(e||!s.isFile()){res.writeHead(404);return res.end('Not found')}res.setHeader('Content-Type',types[path.extname(f)]||'application/octet-stream');res.setHeader('Cache-Control','no-cache');res.setHeader('Content-Length',s.size);fs.createReadStream(f).on('error',()=>res.destroy()).pipe(res)})});
server.on('error',error=>{console.error(error.code==='EADDRINUSE'?'Port 4173 is already in use. Close the earlier preview terminal, or visit its localhost page.':error.message);process.exitCode=1});
server.listen(4173,'127.0.0.1',()=>{console.log('Tuhu v55: http://localhost:4173');if(process.argv.includes('--open')&&process.platform==='win32')spawn('cmd',['/c','start','','http://localhost:4173/?day=03'],{stdio:'ignore',windowsHide:true}).on('error',()=>{})});